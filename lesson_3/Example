Чтобы активировать виртуальную среду для проекта: venv\Scripts\activate в папке проекта
Чтобы добавить библиотеку в конкретную venv -> перейти в папку проекта pip install (название библиотеки)

Чтобы понять как делать транзакцию автоматизированной надо:
1) Прогнать транзакцию вручную
2) Перейти в блокчейн эксплорер с транзакцией которую сделали руками
3) Смотрим to (куда ушла транзакция)
4) Тип транзакции (EIP-1559 или обычная транзакция)
5) Payable транзакция или нет (value внутри транзакции на эксплорере)
6) Параметры Input Data (смотреть их в эксплорере транзакции, для просмотора надо раскодировать)
7) Получить декодированный ABI контракта to из 2 шага, берется снизу кода контракта
8) Закинуть ABI в data/abis/(название проекта.json) -> uint = int, bytes = str.
Использовать надо как в аби, а не эксплорере
9) Если данные по параметрам скрыты и в транзакции есть только MethodID (сигнатура),
надо использовать contracts.get_signature
10) Чтобы создать файл через windows строку copy con (имя файла.расширение)
    ctrl+c -> enter